spring:
  application:
    name: project-collab
  datasource:
    url: jdbc:h2:mem:projectcollab
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html
